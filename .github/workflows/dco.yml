name: DCO

on:
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: write

jobs:
  dco:
    name: DCO Sign-Off Check
    runs-on: ubuntu-latest
    steps:
      - uses: tim-actions/dco@master
